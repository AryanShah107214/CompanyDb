@page
@model CompanyDb.Pages.Sales.IndexModel

@{
    ViewData["Title"] = "Sales";
}

<h1>Sales</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Sale[0].SaleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sale[0].ItemCost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sale[0].Store)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Sale) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SaleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemCost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Store.StoreID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SaleID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SaleID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SaleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
